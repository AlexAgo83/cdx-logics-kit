interface:
  display_name: "Connector Jira"
  short_description: "Import Jira issues into Logics backlog documents"
  default_prompt: "Use $logics-connector-jira to search Jira issues with JQL and import issues into logics/backlog."
