tier: core
interface:
  display_name: "Bootstrapper"
  short_description: "Initialize Logics folder structure in a new repository"
  default_prompt: "Use $logics-bootstrapper to initialize Logics directories and required baseline files in a repository."
