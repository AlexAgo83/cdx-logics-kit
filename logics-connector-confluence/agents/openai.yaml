interface:
  display_name: "Connector Confluence"
  short_description: "Connect Confluence to the Logics workflow: search pages via"
  default_prompt: "Use $logics-connector-confluence to connect Confluence to the Logics workflow: search pages via CQL and import a page into logics/request/ as a."
