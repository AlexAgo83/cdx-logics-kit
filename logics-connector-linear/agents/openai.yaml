interface:
  display_name: "Connector Linear"
  short_description: "Import Linear issues into Logics backlog documents"
  default_prompt: "Use $logics-connector-linear to list Linear issues and import selected issues into logics/backlog."
